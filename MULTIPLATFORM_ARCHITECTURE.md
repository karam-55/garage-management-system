# بنية Garage Go متعدد المنصات

## نظرة عامة

نظام Garage Go متعدد المنصات يربط بين:
- تطبيق ويندوز (للإدارة والميكانيكيين)
- تطبيق أندرويد (للميكانيكيين والعملاء)
- موقع إلكتروني (للعملاء بشكل أساسي)
- قاعدة بيانات مركزية (PostgreSQL + Prisma)
- واجهات API موحدة (REST/GraphQL)

## المنصات والمستخدمون

### 1. تطبيق ويندوز (Desktop App)
**التقنية:** Electron + React/TypeScript
**المستخدمون:**
- مدير الورشة (Admin)
- المحاسب (Accountant)
- مدير المخزون (Inventory Manager)
- الميكانيكيون (Mechanics)

**الميزات:**
- إدارة شاملة للورشة
- تقارير متقدمة
- إدارة المخزون
- معالجة الفواتير
- إعدادات النظام

### 2. تطبيق أندرويد (Mobile App)
**التقنية:** React Native/Flutter
**المستخدمون:**
- الميكانيكيون (في الموقع)
- العملاء (لحجز الخدمات)

**الميزات للميكانيكيين:**
- عرض بطاقات العمل
- تتبع الوقت
- التقاط الصور
- التواصل مع العملاء
- عرض التقويم

**الميزات للعملاء:**
- حجز المواعيد
- تتبع حالة السيارة
- عرض الفواتير
- الدفع الإلكتروني
- تقييم الخدمة

### 3. الموقع الإلكتروني (Web App)
**التقنية:** React/Vue + TypeScript
**المستخدمون:**
- العملاء (الأساسيون)
- الزوار (للاستعلام)

**الميزات:**
- حجز الخدمات
- عرض الخدمات والأسعار
- تتبع الحجوز
- الدفع الإلكتروني
- التواصل مع الورشة

## نظام الصلاحيات والأدوار

### الأدوار الرئيسية:

#### 1. مدير الورشة (Garage Owner/Admin)
**الصلاحيات:**
- الوصول الكامل لجميع الوظائف
- إدارة المستخدمين والصلاحيات
- عرض التقارير المالية
- إعدادات النظام
- إدارة الورشات المتعددة

**المنصات:** ويندوز + ويب + موبايل

#### 2. الميكانيكي (Mechanic)
**الصلاحيات:**
- عرض بطاقات العمل المكلفة له
- تتبع الوقت والعمل
- استخدام قطع الغيار
- إضافة ملاحظات
- التواصل مع العملاء

**المنصات:** ويندوز + موبايل

#### 3. العميل (Customer)
**الصلاحيات:**
- حجز الخدمات
- عرض حجوزاته
- تتبع حالة السيارة
- الدفع والفواتير
- التواصل مع الورشة

**المنصات:** موبايل + ويب

#### 4. الموظف الإداري (Staff)
**الصلاحيات:**
- استقبال الحجوز
- إدارة العملاء
- إصدار الفواتير
- إدارة المخزون (محدود)

**المنصات:** ويندوز + ويب

#### 5. المحاسب (Accountant)
**الصلاحيات:**
- إدارة الفواتير والمدفوعات
- التقارير المالية
- إدارة المصروفات
- تقارير الضرائب

**المنصات:** ويندوز فقط

## بنية النظام التقنية

### Backend (مشترك لكل المنصات)
```
garage-go-backend/
├── src/
│   ├── controllers/     # Controllers لكل المنصات
│   ├── models/       # نماذج البيانات
│   ├── middleware/   # الصلاحيات والتحقق
│   ├── routes/       # مسارات API
│   └── services/     # خدمات الأعمال
├── prisma/
│   └── schema.prisma # قاعدة البيانات
└── docsml.yaml         # وثائق API
```

### Frontend المنصات

#### تطبيق ويندوز (Electron)
```
garage-go-desktop/
├── src/
│   ├── components/   # مكونات React
│   ├── pages/        # الصفحات
│   ├── hooks/        # Hooks مخصصة
│   └── services/     # الاتصال بالـ API
├── public/
└── package.json
```

#### تطبيق أندرويد (React Native)
```
garage-go-mobile/
├── src/
│   ├── screens/      # الشاشات
│   ├── components/   # المكونات
│   ├── navigation/   # التنقل
│   └── services/     # الخدمات
├── android/
└── ios/
```

#### موقع إلكتروني (React)
```
garage-go-web/
├── src/
│   ├── components/   # مكونات
│   ├── pages/        # الصفحات
│   ├── hooks/        # Hooks
│   └── services/     # الخدمات
├── public/
└── package.json
```

## قاعدة البيانات المركزية

### الجداول الرئيسية:
```sql
-- المستخدمون والأدوار
Users
Roles
UserRoles
Permissions

-- بيانات الورشات
Garages
GarageSettings

-- حجوز وإدارة
Bookings
JobCards
Services

-- المخزون
Parts
Inventory
PurchaseOrders

-- المالية
Invoices
Payments
Expenses

-- التقارير والتواصل
Notifications
AuditLogs
```

## واجهات API الموحدة

### REST API Structure
```
/api/v1/
├── auth/           # المصادقة
├── users/          # المستخدمون
├── garages/        # الورشات
├── bookings/       # الحجوز
├── job-cards/      # بطاقات العمل
├── inventory/      # المخزون
├── invoices/       # الفواتير
├── reports/        # التقارير
└── notifications/  # الإشعارات
```

### WebSocket للتحديثات الحية
- تحديثات حالة الحجوز
- إشعارات فورية
- تتبع الوقت المباشر
- تحديثات المخزون

## نظام الصلاحيات التفصيلي

### مستويات الصلاحيات:

#### 1. مستوى النظام (System Level)
- إدارة المستخدمين
- إعدادات النظام
- النسخ الاحتياطي
- التقارير الشاملة

#### 2. مستوى الورشة (Garage Level)
- إدارة الورشة
- إدارة الموظفين
- التقارير المالية
- إعدادات الخدمات

#### 3. مستوى العمليات (Operations Level)
- إدارة الحجوز
- بطاقات العمل
- إدارة المخزون
- الفواتير

#### 4. مستوى العميل (Customer Level)
- حجز الخدمات
- عرض الحجوز
- الدفعات
- التواصل

## المزامنة والبيانات

### استراتيجية المزامنة:
1. **Online Mode:** بيانات حية من الخادم
2. **Offline Mode:** تخزين محلي مؤقت
3. **Sync Mode:** مزامنة عند الاتصال

### البيانات المشتركة:
- بيانات العملاء
- بيانات الخدمات
- حالة الحجوز
- بيانات المخزون

### البيانات المحلية:
- إعدادات التطبيق
- البيانات المؤقتة
- الكاش
- الصور والملفات

## الأمان والخصوصية

### المصادقة:
- JWT Tokens
- Refresh Tokens
- Multi-Factor Authentication
- Session Management

### التشفير:
- HTTPS/WSS
- Data Encryption
- Password Hashing
- API Rate Limiting

### الصلاحيات:
- Role-Based Access Control
- Permission-Based Features
- Data Access Levels
- Audit Logging

## خطة التنفيذ

### المرحلة 1: الأساسيات (4-6 أسابيع)
1. تصميم قاعدة البيانات
2. إنشاء Backend API
3. نظام المصادقة والصلاحيات
4. اختبارات الوحدة

### المرحلة 2: تطبيق ويندوز (6-8 أسابيع)
1. واجهة المستخدم الرئيسية
2. إدارة الحجوز والعملاء
3. تقارير وإحصائيات
4. إعدادات النظام

### المرحلة 3: تطبيق أندرويد (6-8 أسابيع)
1. واجهة الميكانيكي
2. واجهة العميل
3. تتبع الوقت والعمل
4. إشعارات فورية

### المرحلة 4: الموقع الإلكتروني (4-6 أسابيع)
1. واجهة العميل الرئيسية
2. حجز الخدمات
3. تتبع الحالة
4. الدفع الإلكتروني

### المرحلة 5: التكامل والاختبار (4 أسابيع)
1. اختبارات التكامل
2. اختبارات الأداء
3. اختبارات الأمان
4. النشر والتشغيل

## التقنيات المقترحة

### Backend:
- Node.js + TypeScript
- Express.js/Fastify
- Prisma ORM
- PostgreSQL
- Redis (للكاش)
- Socket.io (WebSocket)

### Frontend:
- React + TypeScript
- Material-UI/Ant Design
- Redux/Zustand (State Management)
- React Query (API Calls)

### Mobile:
- React Native + TypeScript
- React Navigation
- Native Base/React Native Paper
- Push Notifications

### Desktop:
- Electron + React
- Electron Builder
- Auto Updates
- Native APIs

### DevOps:
- Docker
- GitHub Actions
- AWS/Azure
- SSL Certificates
